/**
 * Created by tom on 6/17/16.
 */

import { who, where, when } from './es6-module-lib-1';
console.log(`${who} went ${where} on ${when}`);

import { nearly_pi, square } from './es6-module-lib-2';
console.log("pi squared is nearly", square(nearly_pi));

